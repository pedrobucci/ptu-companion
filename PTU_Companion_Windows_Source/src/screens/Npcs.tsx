import { useState } from 'react'
import { Chip, PrimaryButton, Section } from '../components/UI'
const npcs=[{name:'Dr. Marlowe',role:'Researcher',tag:'Merchant',tone:'yellow',notes:['Knows the ruins near Route 12.','Interested in ancient Poké Balls.']},{name:'Captain Vega',role:'Company Supervisor',tag:'Organization',tone:'blue',notes:['Authorized use of company roster.','Strict about mission protocol.']},{name:'Lira',role:'Ranger',tag:'Ally',tone:'green',notes:['Helped the team cross the canyon.']},{name:'Rook',role:'Informant',tag:'Suspect',tone:'purple',notes:['Seen near the old warehouse.']}]
export default function Npcs(){const [idx,setIdx]=useState(0);const n=npcs[idx];return <div className="page"><div className="page-heading"><div><p className="eyebrow">NPC JOURNAL</p><h1>People & Campaign Notes</h1><p>Optional portraits, tags, descriptions and free-form notes.</p></div><PrimaryButton>＋ Add NPC</PrimaryButton></div><div className="npc-layout"><Section title="NPCS"><div className="npc-list">{npcs.map((x,i)=><button className={i===idx?'active':''} onClick={()=>setIdx(i)} key={x.name}><div className="npc-avatar">{x.name.split(' ').map(p=>p[0]).join('').slice(0,2)}</div><div><strong>{x.name}</strong><small>{x.role}</small></div><Chip tone={x.tone as any}>{x.tag}</Chip></button>)}</div></Section><Section title="NPC DETAILS"><div className="npc-hero"><div className="npc-avatar big">{n.name.split(' ').map(p=>p[0]).join('').slice(0,2)}</div><div><h2>{n.name}</h2><p>{n.role}</p><Chip tone={n.tone as any}>{n.tag}</Chip></div><div className="relation">♥ ♥ ♥ ♥ ♡</div></div><div className="npc-about"><h3>About</h3><p>Campaign note placeholder. This region can hold a longer character description, affiliations, remembered details and GM/player observations.</p><dl><div><dt>Affiliation</dt><dd>Independent</dd></div><div><dt>Last Seen</dt><dd>Seaport Town — East Dock</dd></div><div><dt>First Met</dt><dd>Session 04</dd></div></dl></div></Section><Section title="NOTES"><div className="sticky-grid">{n.notes.map((x,i)=><div className={`sticky s${i%4}`} key={x}>{x}<small>Session note</small></div>)}<button className="sticky add">＋ Add note</button></div></Section></div></div>}
