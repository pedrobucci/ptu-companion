# Fakemon 1 leva import — v1.4

Source: user-supplied `Fakemon 1 leva.pdf`.

Imported Species/Form logical IDs:

| Logical ID | Display name | Type | Source page |
|---|---|---|---:|
| `panthore` | Panthore | Electric / Dark | 1 |
| `panzeus` | Panzeus | Electric / Dark | 2 |
| `clefable-w` | Clefable W. | Ghost / Normal | 3 |
| `clefable-k` | Clefable K. | Electric / Fighting | 4 |

The pack ID is `campaign-homebrew-fakemon-1-leva` and is enabled in `all-provided-material`.

Panthore → Panzeus uses the PTU source level 20.

The source pages for Clefable W./K. each print `Cleffa → Clefairy Minimum 10 → Clefable Minimum 20`. The app gives the two supplied final variants stable IDs (`clefable-w`, `clefable-k`) while retaining the raw source wording in their definition metadata.

Source spelling/layout anomalies are either explicitly normalized when the boundary is unambiguous (for example a concatenated tutor-list token) or retained as unresolved references marked for review.
